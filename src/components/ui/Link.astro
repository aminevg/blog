---
import type { HTMLAttributes } from "astro/types";

type Props = HTMLAttributes<"a">;

const { class: className, ...rest } = Astro.props;
---

<a
  {...rest}
  class:list={["hover:text-oxblood focus-ring text-inherit", className]}
  ><slot /></a
>
